{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "Live polling cadence: every 2 minutes during the tournament. Vercel cron uses standard cron syntax (UTC). Tune the schedule down (e.g. '*/5 * * * *') outside live windows to save invocations; app_settings.live_polling_seconds documents the intended in-match cadence. The cron route is bearer-authenticated with CRON_SECRET.",
  "crons": [
    {
      "path": "/api/cron/update-results",
      "schedule": "*/2 * * * *"
    }
  ]
}
